// Feature flags configuration
// This file centralizes all feature flags for the application

const features = {
  // Controls which pages are accessible
  // true: all pages are accessible
  // false: only home page is accessible, other routes redirect to home
  SHOW_ALL_PAGES: false,
};

export default features;
